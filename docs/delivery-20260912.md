# Delivery record - 2026-09-12 integration round

All times Asia/Taipei. This record separates what passed, what is a partial delivery,
and what stays blocked. Nothing here upgrades a draft into a formal valuation.

## Version under test

- Branch `feat/team-integration`, Draft PR [#59](https://github.com/chengruchou/newtaipei-appraisal-review-ai/pull/59)
- Integrates: #55 workbench `728f40c` + #57 `649d514` + #58 `85d02eb` + #56 `0c3daae`
  plus the export-chain delivery (see PR head for the exact SHA; every test below names
  the local head it ran at).

## Deployed demo

- URL: `http://35.93.63.186` (EC2 `i-0592fb65ce4c38cc0`, us-west-2, account 242971039848)
- Architecture: one container from `deploy/Dockerfile` target `workbench` - the API stays
  on the container's loopback with its bearer-token authority; nginx publishes only the
  frontend origin on port 80. The security group exposes port 80 alone.
- Sign-in: the launcher-issued session token, returned privately through
  `s3://s3bucket-242971039848-us-west-2-an/deploy/instance-fixture.json` (never in Git or
  this PR). Fetch it with team credentials and paste it on the sign-in page.
- State: `/srv/workbench` on the instance; container restart reopens, never resets.
- Known limitation: HTTP only (no TLS) - demo scope; noted, not hidden.

## What was actually verified

### Local, over real HTTP (25/25 checks, exit 0)

login rejected without token (403) -> authorized reads -> exports/basis serves the
registered snapshot digest and template bundle -> POST exports (xlsx) 202 -> worker
fills three official tables -> exact replay returns the same operation -> same key with
changed format 409 -> authorized downloads, sha256 equal to the manifest hash ->
workbooks reopen (24 sheets intact, only the visible sheet written) -> POST exports
(pdf) converts the same filled workbook bytes via headless LibreOffice -> one page per
table, hidden legacy sheets pruned from the rasterized copy only -> unauthorized
download 403 throughout. Restart recovery and stale-revision 409 were verified on the
task plane earlier the same day (13/13 acceptance probe).

### In the deployment container (linux/amd64)

prepare -> automatic snapshot registration (208 entries, 568 gaps) -> PDF export
succeeded 3/3 -> download hash verified -> render-input hash recorded. Visual check of
表4: full Traditional Chinese headers (Noto CJK), real Shulin subjects (樹德段1415 /
樹德段284 / 太平段367,917 / 文林段317), dates and 差異率 (5.96% / 4.09% / 5.49%),
第一種住宅區, 建蔽率 50%, 容積率 260%/200%/260%, and the A37 note inside the printed
page. Empty cells stay empty.

### Gates at the delivery head

- Python: full non-privacy regression exit 0; ruff + format + mypy --strict clean.
- Frontend: `npm run verify` (typecheck, lint, format, unit suites, build, artifact
  check) - result recorded alongside the final head in the PR conversation.
- `scripts/check_submission.py` passed before every push.

## Real data discipline

- Sources parsed natively (no OCR): assignment + criteria for Shulin, 基準日 2022-09-01,
  P001 comparison base + P002/P003/P004 comparables - one case, four subjects.
- Snapshot: 208 entries (71 given inputs, 104 computed with Decimal traces, 33
  not-applicable), 568 explicit gaps. The organizer's pre-filled sample contributed
  nothing to any value (test-asserted); it sits in `acceptance-reference/` in S3,
  outside every runtime prefix.
- Percentage conventions enforced by type: 表5 stores 5 as 5 points; 表4 renders the
  same stored points as 0.05.
- S3 layout under `cases/shulin-1110901/`: `sources/`, `templates/`, `rules/`,
  `artifacts/`, `source-inventory.json`; every upload read back and hash-matched.

## Partial delivery and blockers (honest list)

1. The demo case shell is the isolated synthetic case bound to the real Shulin
   calculation snapshot; full real-source case registration through the transaction
   plane is wired at the ports but not the demo path. Every output is labelled 草稿.
2. 568 snapshot gaps remain: absent assignment inputs (宗地面積/寬深/臨路 etc.), the
   P001 zoning ambiguity awaiting human confirmation, and subtotal keys held back until
   their conventions are confirmed. Listed by `exports/basis`; rendered as blanks.
3. Formal mode returns 409 by design - no formal approval gate exists this round.
4. Local macOS LibreOffice renders the PDFs without CJK glyphs (host font limitation);
   the container output is the deliverable and renders correctly.
5. CI is not active on the repository; every count above is a local run at a named SHA.
6. Model-candidate extraction against the deployed runtime was not exercised this
   round (AWS credentials arrived 13:59); routing preflight fixes are merged and
   unit-verified.

## Start / stop / recover

- Local: `PYTHONPATH=src .venv/bin/python scripts/run_local_workbench.py --directory
  artifacts/<dir> --port <port>` (dir must be 0700); register a snapshot with
  `scripts/register_prepared_snapshot.py`.
- Container: `docker compose up --build` (loopback) or the cloud path in
  `deploy/README.md`.
- Instance: container restarts itself (`--restart unless-stopped`); state persists in
  `/srv/workbench`. Stop the demo by stopping the instance; nothing else runs there.
EOF
